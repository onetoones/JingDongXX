<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jingdongxx">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- 读取网络状态的权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".parentclass.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".DHActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".my.view.LoginActivity" />
        <activity android:name=".my.view.RegActivity" />
        <activity android:name=".classes.view.GoListActivity" />
        <activity android:name=".classes.view.DetailActivity" />
        <activity android:name=".home.view.iview.ScanActivity" />
        <activity android:name=".home.view.SearchActivity" />
        <activity android:name=".shopping.view.OrderActivity" />
        <activity android:name=".shopping.view.ConfirmActivity" />
        <activity android:name=".my.view.AddressActivity" />
        <activity android:name=".my.view.SeeOrderActivity" />
        <activity android:name=".my.view.SetupActivity"></activity>
    </application>

</manifest>